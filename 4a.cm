int a;

int adder(int x, int y) {
    return x + y;
}

/* make sure this only works with array as second param */
int test(int x, int arr[]) {
    return 4;
}

int main(void) {
    int b; int c; int d[3];
    a = 1;
    b = 2;
    c = 3;

    /*adder(1, adder(a, b));*/
    test(d[4], d);  /*d should work */

    /* Error: function call sends too many/too few arguments */
    /* adder(a); or adder(); etc */
    return adder(a, b, c);


}