/* A program to compute the factorial value of an input */

int global;

void main(void) {
  
  int x; int fac;
  int global;
  x = input();
  fac = 1;
  {
    int x;
  }
  while (x > 1) {
    fac = fac * x;
    x = x - 1;
  } 

  output(fac);
}
