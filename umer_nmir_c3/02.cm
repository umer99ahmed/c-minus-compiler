/*global and local variables with assignment simplevar and constants, and output are working*/
int y;
void main(void) {
    int x;
    x = 2;
    y = x;
    output(x);
    output(y);
    x = x + 3;
    output(x);
}
